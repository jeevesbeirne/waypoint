// Routing is handled by app/_layout.tsx (onboarding gate via useEffect).
// This file is intentionally a neutral passthrough.
import { View } from 'react-native';

export default function Index() {
  return <View />;
}
